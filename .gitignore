# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env
.env*.local
.env.*
# ...but the template is meant to be committed; it holds no real values.
!.env.example

# Common ignores
node_modules
.next/
.DS_Store

# Supabase CLI local cache
supabase/.temp/
# TypeScript incremental build cache
tsconfig.tsbuildinfo

# npm lockfile — this project uses pnpm (see pnpm-lock.yaml)
package-lock.json
